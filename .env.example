# FinanzIA local environment placeholders
# Copy to .env when running the local stack. Never commit real secrets.

NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=replace-with-local-anon-key
SUPABASE_SERVICE_ROLE_KEY=replace-only-for-server-side-local-use

FRONTEND_PORT=3000
OCR_SERVICE_PORT=8010
OCR_SERVICE_URL=http://localhost:8010
